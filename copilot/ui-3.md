* [ ] UI微調整

  - マウスオーバーで変化するスタイルはtransitionは不要です。
    もたついた印象になるので。

  - 選択中のvisitを含むvisit setも強調表示してください。

  - visit setの背景部分のクリックでそのvisit setの最初のvisitを選択するようにしてください。

  - Load more {newer, older} visitボタンのクリック時にはLoadingSpinnerをvisit set list上にオーバーレイで表示するようにしてください。

  - Load more {newer, older} visitボタンは新しいvisit/古いvisitを現在のlistに追加するように動作します。
    その際既存のvisitの表示位置が保存されれば見やすいです。
    既存のプロジェクトの実装を参考にし、そのようにスクロール位置を調整してください。

* [ ] 追加のタスクがないか指示者に確認
