# pfs-obslog フロントエンド 画面構成仕様書

## 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                        App (100vh)                          │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                      Header                            │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                                                        │ │
│  │               Routes (flex-grow: 1)                    │ │
│  │                                                        │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### ルーティング

| パス | ページ | 認証 |
|------|--------|------|
| `/` | → `/visits` へリダイレクト | - |
| `/visits/:visit_id?` | Home (メイン画面) | 必要 |
| `/visits/sql-syntax-help` | SqlSyntaxHelp (検索構文ヘルプ) | 不要 |
| `/login` | Login (ログイン画面) | 不要 |
| `/designs/:design_id?` | Designs (PFS Design画面) | 必要 |
| `/devel` | Devel (開発用、DEVモードのみ) | - |

---

## Header コンポーネント

**ファイル**: `src/pages/Header/index.tsx`

```
┌─────────────────────────────────────────────────────────────┐
│ PFS-OBSLOG  │  {username}  │   (spacer)   │  📷  🎨  ⚙️  🚪  │
│   (logo)    │   (user)     │              │   (buttons)     │
└─────────────────────────────────────────────────────────────┘
```

### 構成要素

| 要素 | 説明 | 動作 |
|------|------|------|
| ロゴ | "PFS-OBSLOG" テキスト | 表示のみ |
| ユーザー名 | 現在ログインユーザー | ログイン時のみ表示 |
| Visits ボタン (📷) | カメラアイコン | `/visits` へ遷移 |
| Designs ボタン (🎨) | design_services アイコン | `/designs` へ遷移 |
| 設定ボタン (⚙️) | settings アイコン | `/devel` へ遷移 (DEVモードのみ) |
| ログアウトボタン (🚪) | logout アイコン | ログアウト処理 |

---

## Login ページ

**ファイル**: `src/pages/Login/index.tsx`

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    ┌─────────────────┐                      │
│                    │   Login Form    │                      │
│                    │                 │                      │
│                    │  STN Account:   │                      │
│                    │  [___________]  │                      │
│                    │                 │                      │
│                    │  Password:      │                      │
│                    │  [___________]  │                      │
│                    │                 │                      │
│                    │      [Log In]   │                      │
│                    └─────────────────┘                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 構成要素

| 要素 | 説明 |
|------|------|
| STN Account入力 | ユーザー名入力 (autocomplete="username") |
| Password入力 | パスワード入力 (autocomplete="current-password") |
| Log Inボタン | ログイン送信 |

---

## Home ページ (Visit一覧・詳細)

**ファイル**: `src/pages/Home/index.tsx`

```
┌─────────────────────────────────────────────────────────────┐
│                        HomeContext                          │
│  ┌──────────────────────┬───┬─────────────────────────────┐ │
│  │                      │ G │                              │ │
│  │    VisitSetList      │ u │       VisitDetail            │ │
│  │    (左ペイン)         │ t │       (右ペイン)             │ │
│  │                      │ t │                              │ │
│  │                      │ e │                              │ │
│  │                      │ r │                              │ │
│  └──────────────────────┴───┴─────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### レイアウト

- **Split Grid**: 水平方向に分割可能なペイン
- **初期比率**: `1fr 8px 1fr` (左:ガター:右 = 1:固定:1)
- **ガター**: ドラッグで幅調整可能

---

## VisitSetList (左ペイン)

**ファイル**: `src/pages/Home/VisitSetList/index.tsx`

```
┌──────────────────────────────────────┐
│              ToolBar                  │
│  ┌──────────────────────────────────┐│
│  │ SearchTextBox [検索入力] [🔍][?] ││
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │ SearchConditions                  ││
│  │ #SpS:[>=0▼] #MCS:[>=0▼] #AGC:... ││
│  │ 📅 [日付範囲ピッカー]        [↗][📄][{}]││
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │ Columns  [✓ID][✓📝][✓📅]...      ││
│  │ Paginator [⏮][◀] 1-50/1000 [▶][⏭]││
│  └──────────────────────────────────┘│
├──────────────────────────────────────┤
│              SearchResult             │
│  ┌──────────────────────────────────┐│
│  │ [◀][↑/🔄][▶]                     ││  ← LoadMoreNewer
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │         VisitGroup 1              ││
│  │  ┌────────────────────────────┐  ││
│  │  │     IicSequence Header      │  ││
│  │  │  ID - Name  [Group=xxx]     │  ││
│  │  │  ┌────────────────────────┐│  ││
│  │  │  │VisitSetID|Name|Type|St.││  ││
│  │  │  └────────────────────────┘│  ││
│  │  │  $ command_string           │  ││
│  │  │  [comments]                 │  ││
│  │  │  [NoteList]                 │  ││
│  │  └────────────────────────────┘  ││
│  │  ┌────────────────────────────┐  ││
│  │  │       VisitTable            │  ││
│  │  │ ID|📝|📅|⏰|#|⏱|...         │  ││
│  │  │ -------------------------   │  ││
│  │  │ 12345|desc|12/30|10:00|3/0/0│  ││
│  │  │ 12344|    |12/30|09:55|2/1/0│  ││
│  │  └────────────────────────────┘  ││
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │         VisitGroup 2              ││
│  │  ...                              ││
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │ [◀][↓][▶]                        ││  ← LoadMoreOlder
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

### ToolBar 詳細

#### SearchTextBox
- 検索入力フィールド (type="search")
- `where`で始まる場合はSQL検索モード (スタイル変更)
- 通常テキストは `where any_column like '%{text}%'` に変換
- ボタン: 検索実行 (🔍), 構文ヘルプ (?)

#### SearchConditions
| 要素 | 説明 |
|------|------|
| #SpS, #MCS, #AGC | 露出数フィルター (>=0, >0, ==0) |
| DateRangePicker | 日付範囲選択 |
| Go to Visit (↗) | Visit ID入力でジャンプ |
| Download CSV (📄) | CSV形式でダウンロード |
| Download JSON ({}) | JSON形式でダウンロード |

#### Columns (表示カラム選択)
チェックボックスで各カラムの表示/非表示を切り替え:

| カラムキー | アイコン/ラベル | 説明 |
|-----------|---------------|------|
| id | ID | Visit ID |
| description | 📝 | 説明 |
| issuedAtDate | 📅 | 発行日 |
| issuedAtTime | ⏰ | 発行時刻 |
| numberOfExposures | # | SpS/MCS/AGC露出数 |
| exposureTime | ⏱ | 平均露出時間 |
| eet_b, eet_r, eet_n, eet_m | (b)(r)(n)(m) | 有効露出時間 |
| pfs_design_id | 🎨 | PFS Design ID |
| seeingMedian | ◆ | シーイング中央値 |
| transparancyMedian | ☁ | 透過率中央値 |
| coord_a, coord_d | α, δ | 赤経・赤緯 |
| azimuth, elevation | A°, E° | 方位角・仰角 |
| insturumentRotator | I° | 回転角 |
| notes | 📝 | ノート |

#### Paginator
| ボタン | 動作 |
|--------|------|
| ⏮ (first_page) | 最新Visitへ |
| ◀ (navigate_before) | 前ページ |
| 表示範囲 | "1 - 50 / 1000" |
| ▶ (navigate_next) | 次ページ |
| ⏭ (last_page) | 最古Visitへ |
| 🔄 (autorenew/refresh) | 自動更新 (15秒間隔) |

### VisitGroup 詳細

#### IicSequence ヘッダー
```
┌────────────────────────────────────────┐
│ {visit_set_id} — {name} [Group={name}] │
├────────────────────────────────────────┤
│ Visit Set ID | Name | Type | Status    │
│ ─────────────────────────────────────  │
│ 123          | xxx  | bias | finished  │
├────────────────────────────────────────┤
│ $ iicActor command --args              │
├────────────────────────────────────────┤
│ [comments]                             │
├────────────────────────────────────────┤
│ [NoteList - ノート一覧・追加]          │
└────────────────────────────────────────┘
```

#### VisitTable
| 列 | 内容 |
|----|------|
| ID | Visit ID (右寄せ) |
| 📝 | 説明 (ツールチップ) |
| 📅 | 発行日 |
| ⏰ | 発行時刻 |
| # | SpS/MCS/AGC露出数 (色付き) |
| ⏱ | 平均露出時間 |
| ... | その他選択されたカラム |

---

## VisitDetail (右ペイン)

**ファイル**: `src/pages/Home/VisitDetail/index.tsx`

```
┌────────────────────────────────────────┐
│            VisitDetailContext          │
│  ┌──────────────────────────────────┐ │
│  │                                   │ │
│  │         VisitInspector            │ │
│  │                                   │ │
│  │                                   │ │
│  ├───────────────────────────────────┤ │  ← Gutter (水平ドラッグ)
│  │                                   │ │
│  │         FitsHeaderInfo            │ │
│  │         (高さ: 250px初期)          │ │
│  │                                   │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

### レイアウト
- **Split Grid**: 垂直方向に分割可能
- **初期比率**: `1fr 8px 250px` (上:ガター:下)

---

## VisitInspector

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/index.tsx`

```
┌────────────────────────────────────────┐
│              Summary                    │
│  ┌────┬────┬───────┬───────┬─────────┐│
│  │ ID │ 📝 │  ⏰   │  #    │  💬     ││
│  ├────┼────┼───────┼───────┼─────────┤│
│  │[↗]123│desc│2024..│3/0/0 │[NoteList]││
│  └────┴────┴───────┴───────┴─────────┘│
├────────────────────────────────────────┤
│  [SpS][MCS][AGC][IIC Sequence][Seq.Grp]│  ← Tabs
├────────────────────────────────────────┤
│                                        │
│           Tab Contents                 │
│                                        │
└────────────────────────────────────────┘
```

### Summary (グリッドレイアウト)

| ヘッダー | 内容 |
|----------|------|
| ID | Visit ID (左リストへジャンプボタン付き) |
| 📝 (Description) | 説明文 |
| ⏰ (Issued at) | 発行日時 |
| # (Exposures) | SpS/MCS/AGC露出数 |
| 💬 (Notes) | NoteList コンポーネント |

### Tabs

| タブ | コンポーネント | 条件 |
|------|---------------|------|
| SpS | SpsInspector | sps データあり |
| MCS | McsInspector | mcs データあり |
| AGC | AgcInspector | agc データあり |
| IIC Sequence | IicSequence | iic_sequence データあり |
| Sequence Group | SequenceGroup | iic_sequence.group データあり |

---

## SpsInspector (SpSタブ)

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/SpsInspector/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ Summary                          │  │
│  │  Type  | # Exposures | Avg Time  │  │
│  │  bias  | 4           | 0.00      │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ Settings                         │  │
│  │ Image Type: ○Raw ○postISRCCD    │  │
│  │ Image Size: ○Small ○Medium ○Large│  │
│  │ Download All [Raw] [Post ISR]    │  │
│  └─────────────────────────────────┘  │
├────────────────────────────────────────┤
│  Exposures Table (arm × module)        │
│  ┌─────┬─────┬─────┬─────┬─────┐      │
│  │     │  1  │  2  │  3  │  4  │      │
│  ├─────┼─────┼─────┼─────┼─────┤      │
│  │  n  │[img]│[img]│[img]│[img]│      │
│  │  r  │[img]│[img]│[img]│[img]│      │
│  │  m  │[img]│[img]│[img]│[img]│      │
│  │  b  │[img]│[img]│[img]│[img]│      │
│  └─────┴─────┴─────┴─────┴─────┘      │
│                                        │
│  各[img]にボタン: [📊][⬇]             │
│  📊: FITSヘッダー表示                   │
│  ⬇: FITSファイルダウンロード            │
└────────────────────────────────────────┘
```

### SpsExposure 個別
- **画像**: LazyImage (PNG プレビュー)
- **サイズ**: Small(0.5x), Medium(1x), Large(2x) × 300px
- **ボタン**: FITSヘッダー表示、ダウンロード

---

## McsInspector (MCSタブ)

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/McsInspector/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ Summary: # Exposures | Avg Time  │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ Settings                         │  │
│  │ Image Type: ☑Plot ☐Raw          │  │
│  │ Image Size: ○Small ○Medium ○Large│  │
│  └─────────────────────────────────┘  │
├────────────────────────────────────────┤
│  Exposures List (スクロール可能)        │
│  ┌──────────────────────────────────┐ │
│  │ Frame ID = 12345                  │ │
│  │ ┌────────┐ ┌────────┐            │ │
│  │ │ [Plot] │ │ [Raw]  │            │ │
│  │ └────────┘ └────────┘            │ │
│  │           [📊][⬇][👁][{}]         │ │
│  └──────────────────────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │ Frame ID = 12344                  │ │
│  │ ...                               │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

### ボタン
- 📊 (view_column): FITSヘッダー表示
- ⬇ (file_download): FITSダウンロード
- 👁 (visibility): 大きいプレビュー (新しいウィンドウ)
- {} (data_object): JSON詳細表示

---

## AgcInspector (AGCタブ)

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/AgcInspector/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ Summary: # Exposures | Avg Time  │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ Size: ○Small ○Medium ○Large      │  │
│  └─────────────────────────────────┘  │
├────────────────────────────────────────┤
│  ┌─────┐  ┌────────────────────────┐  │
│  │Page │  │ Exposures              │  │
│  │List │  │ ┌────────────────────┐ │  │
│  │     │  │ │Exposure ID = 123   │ │  │
│  │ 1-20│  │ │ ┌───┬───┬───┐      │ │  │
│  │21-40│  │ │ │[1]│[2]│[3]│      │ │  │
│  │41-60│  │ │ ├───┼───┼───┤      │ │  │
│  │ ... │  │ │ │[4]│[5]│[6]│      │ │  │
│  │     │  │ │ └───┴───┴───┘      │ │  │
│  │     │  │ │     [📊][⬇][{}]    │ │  │
│  │     │  │ └────────────────────┘ │  │
│  └─────┘  └────────────────────────┘  │
└────────────────────────────────────────┘
```

### 特徴
- **ページネーション**: 20件ずつ表示 (左側セレクトボックス)
- **6カメラ表示**: 各Exposureで6枚の画像 (HDU index 1-6)
- **スケール**: Small(0.5x), Medium(0.67x), Large(1x)

---

## IicSequence タブ

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/IicSequence/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ Visit Set ID | Name | Type | St │  │
│  │ ─────────────────────────────── │  │
│  │ 123          | xxx  | bias | ok │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ $ iicActor command --args       │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ [comments text]                 │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ [NoteList]                      │  │
│  └─────────────────────────────────┘  │
└────────────────────────────────────────┘
```

---

## SequenceGroup タブ

**ファイル**: `src/pages/Home/VisitDetail/VisitInspector/SequenceGroup/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ Group ID | Name     | Created   │  │
│  │ ──────────────────────────────  │  │
│  │ 4        | RSC_M39_01| 2022-... │  │
│  └─────────────────────────────────┘  │
└────────────────────────────────────────┘
```

---

## FitsHeaderInfo

**ファイル**: `src/pages/Home/VisitDetail/FitsHeaderInfo/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  │
│  │ filename.fits  [0][1][2][3]...  │  │  ← HDU選択ボタン
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │     Key    |  Value  | Comment  │  │
│  │ [________] |[______]|[________]│  │  ← フィルター入力
│  │ ─────────────────────────────── │  │
│  │ SIMPLE     | T       | ...      │  │
│  │ BITPIX     | 16      | ...      │  │
│  │ NAXIS      | 2       | ...      │  │
│  │ ...        | ...     | ...      │  │
│  │            (スクロール可能)       │  │
│  └─────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 機能
- **HDU選択**: ボタンで切り替え
- **フィルター**: Key/Value/Commentで正規表現検索
- **localStorage保存**: 検索条件、選択HDUを記憶

---

## NoteList コンポーネント

**ファイル**: `src/pages/Home/NoteList/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌──────────────────────────────────┐ │
│  │ Note body text         [✏][🗑]  │ │  ← 自分のノート: 編集・削除可
│  └──────────────────────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │ Other's note           username │ │  ← 他人のノート: 表示のみ
│  └──────────────────────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │ [新規ノート入力...         ] [+] │ │  ← 新規追加
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

### 編集モード
```
┌────────────────────────────────────────┐
│ [編集中のテキスト入力........] [✓][✗] │
└────────────────────────────────────────┘
```

---

## Designs ページ (PFS Design画面)

**ファイル**: `src/pages/Designs/index.tsx`

```
┌─────────────────────────────────────────────────────────────┐
│                     Designs2Provider                         │
│  ┌───────────────┬─────────────────────────────────────────┐│
│  │               │                                          ││
│  │  DesignList   │            SkyViewer                     ││
│  │  (左ペイン)    │            (天球表示)                     ││
│  │               │                                          ││
│  │               ├──────────────────────────────────────────┤│
│  │               │                                          ││
│  │               │          DesignDetail                    ││
│  │               │          (詳細情報)                       ││
│  │               │                                          ││
│  └───────────────┴─────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### レイアウト
- **左**: DesignList (固定幅)
- **右上**: SkyViewer (flex-grow)
- **右下**: DesignDetail

---

## DesignList

**ファイル**: `src/pages/Designs/DesignList/index.tsx`

```
┌────────────────────────────┐
│  ┌────────────────────┐   │
│  │ [検索入力...    ][🔄]│   │
│  └────────────────────┘   │
│  ┌────────────────────┐   │
│  │ID Format: ○Hex ○Dec│   │
│  │Sort: ○Alt ○DateMod │   │
│  └────────────────────┘   │
├────────────────────────────┤
│  ┌────────────────────┐   │  ← EntryGroup
│  │ ┌──────────────────┐│   │
│  │ │ Design Name      ││   │
│  │ │ 0x1234abcd...    ││   │
│  │ │ 2024-12-30       ││   │
│  │ │ 100/50/30/200/10 ││   │  ← science/sky/flux/photo/guide
│  │ │ α=180°, δ=45°    ││   │
│  │ │ Alt.=75°     [⬇] ││   │
│  │ └──────────────────┘│   │
│  │ ┌──────────────────┐│   │
│  │ │ (同一位置の別Design) ││   │
│  │ └──────────────────┘│   │
│  └────────────────────┘   │
│  ┌────────────────────┐   │
│  │ ...                 │   │
│  └────────────────────┘   │
└────────────────────────────┘
```

### 機能
- **検索**: 名前/IDで正規表現フィルタ
- **ID形式**: Hex/Decimal切り替え
- **ソート**: 高度順/更新日順
- **グルーピング**: 近い座標のDesignをグループ化
- **選択**: クリックで選択→天球でジャンプ
- **ホバー**: マウスオーバーでハイライト

---

## SkyViewer

**ファイル**: `src/pages/Designs/SkyViewer/index.tsx`

```
┌──────────────────────────────────────────┐
│                                          │
│           StellarGlobe                   │
│           (天球3D表示)                    │
│                                          │
│    ┌─ DesignCircles (Design位置マーカー)  │
│    └─ DesignDetail (選択Design表示)      │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ [日付ピッカー] [🕐時計] [Now][Center]│ │
│  │ ☑Fiber Markers                     │ │
│  └────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

### コントロール
| 要素 | 説明 |
|------|------|
| DatePicker | 日付選択 (HST) |
| Clock | 時刻調整 (ドラッグで変更) |
| Set time to now | 現在時刻に設定 |
| Center Zenith | 天頂を中心に |
| Fiber Markers | ファイバー位置マーカー表示切替 |

---

## DesignDetail (Designs内)

**ファイル**: `src/pages/Designs/DesignDetail/index.tsx`

```
┌──────────────────────────────────────────┐
│  ┌────────────────┐  ┌────────────────┐ │
│  │                │  │                 │ │
│  │   FocalPlane   │  │   FiberDetail   │ │
│  │   (焦点面図)    │  │   or            │ │
│  │                │  │   DesignSummary │ │
│  │                │  │                 │ │
│  └────────────────┘  └────────────────┘ │
│  ┌──────────────────────────────────┐   │
│  │ Color: [Target Type ▼]           │   │
│  │ ┌─Legend────────────────────┐    │   │
│  │ │ ● SCIENCE  ● SKY  ● FLUX  │    │   │
│  │ └───────────────────────────┘    │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

### FocalPlane
- **サイズ**: 250px
- **インタラクション**: ホバーでFiber詳細表示
- **色分け**: Target Type / Fiber Status

### FiberDetail (ホバー時)
| セクション | 内容 |
|-----------|------|
| Fiber | Cobra ID, Fiber ID, Module ID, Sector ID |
| Design | catId, Tract/Patch, objId, α, δ, Target Type, Fiber Status, pfiNominal |
| Photometry | filterName, fiberFlux, psfFlux, totalFlux等 |

### DesignSummary (非ホバー時)
| 項目 | 内容 |
|------|------|
| Name | デザイン名 |
| Modified | 更新日時 |
| α, δ | 座標 |
| Position Angle | 位置角 |
| Arms | 使用アーム |

---

## SqlSyntaxHelp ページ

**ファイル**: `src/pages/Home/SqlSyntaxHelp/index.tsx`

```
┌────────────────────────────────────────┐
│  ┌──────────────────────────────────┐ │
│  │ Search Query Syntax              │ │
│  │                                  │ │
│  │ ## Virtual Table                 │ │
│  │ ┌─────────────────────────────┐ │ │
│  │ │ Name | OPDB Column | Doc    │ │ │
│  │ │ ──────────────────────────  │ │ │
│  │ │ id   | pfs_visit_id | ...   │ │ │
│  │ │ ...  | ...          | ...   │ │ │
│  │ └─────────────────────────────┘ │ │
│  │                                  │ │
│  │ ## Examples                      │ │
│  │ - Title: SQL query               │ │
│  │   説明                           │ │
│  │ - ...                            │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

---

## 共通コンポーネント

### Tabs

```
┌────────────────────────────────────────┐
│ [Tab1] [Tab2] [Tab3*] [Tab4]          │  ← タブバー (*=選択中)
├────────────────────────────────────────┤  ← ガター
│                                        │
│           Tab Contents                 │  ← コンテンツ領域
│                                        │
└────────────────────────────────────────┘
```

### Layout コンポーネント

| コンポーネント | CSS |
|---------------|-----|
| Flex | `display: flex` |
| FlexColumn | `display: flex; flex-direction: column` |
| FlexPadding | `flex-grow: 1` |
| Center | 中央揃え |
| JustifyEnd | `justify-content: flex-end` |
| GridCellGroup | グリッドセルグループ |

### Icon / IconButton

- Material Icons使用
- `<Icon icon="icon_name" />`
- `<IconButton icon="icon_name" onClick={...} tippy={{content: 'tooltip'}} />`

### LazyImage

- 遅延読み込み画像
- スケルトン表示 (読み込み中)
- IntersectionObserverで可視時に読み込み

### Loading / Block

- `<Loading />`: スピナー表示
- `<Block when={condition}>`: 条件付きオーバーレイ

### DatePicker / DateRangePicker

- vanillajs-datepicker ラッパー
- 単一日付/範囲選択

### Tippy

- tippy.js ラッパー
- `use:tippy={{ content: 'tooltip text' }}`

---

## 状態管理 (Context)

### HomeContext
```typescript
{
  visitId: Signal<number | undefined>
  setVisitId: Setter
  refreshHomeSignal: Signal<RefreshParams>
  refreshHome: (params) => Promise
  goToVisit: (params) => void
  goToVisitSignal: Signal<GotoVisitParams>
}
```

### VisitSetListContext
```typescript
{
  groups: Signal<VisitGroupType[]>
  isLoading: Signal<boolean>
  queryParams: Signal<QueryParams>
  setQueryParams: {...}
  refresh: (showLoader?) => Promise
  columns: Signal<ColumnFlags>
  setColumns: Setter
  visitCount: Signal<number>
  scrollElement: Element
}
```

### VisitDetailContext
```typescript
{
  fitsId: Signal<FitsId | undefined>
  setFitsId: Setter
}
```

### Designs2Context
```typescript
{
  designs: Resource<PfsDesignEntry[]>
  selectedDesign: Signal<PfsDesignEntry | undefined>
  focusedDesign: Signal<PfsDesignEntry | undefined>
  designDetail: Resource<DesignDetailType>
  jumpTo: (camera, coord) => void
  zenithSkyCoord: Signal<SkyCoord>
  now: Signal<Date>
  showFibers: Signal<boolean>
  telescopeLocation: Signal<Location>
}
```
